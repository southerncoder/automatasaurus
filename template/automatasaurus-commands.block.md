## Automatasaurus Project Commands

Update these commands to match this repository.

| Action | Command |
|--------|---------|
| Install dependencies | `npm install` |
| Run tests | `npm test` |
| Lint | `npm run lint` |
| Build | `npm run build` |

Notes:
- This file is used by Copilot agents to choose the right project commands.
- Put custom content outside the managed block.
